[project]
name = "bern-02"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "claramariekatharina.2013@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.11.1",
    "numpy>=2.4.6",
    "pandas>=3.0.5",
    "scipy>=1.17.1",
]

[project.scripts]
bern-02 = "bern_02:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
